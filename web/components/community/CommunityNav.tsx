import AppNavigation from "@/components/navigation/AppNavigation";

/** @deprecated Use AppNavigation directly for the shared authenticated shell. */
export default function CommunityNav() {
  return <AppNavigation />;
}
